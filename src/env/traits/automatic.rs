use crate::error::AmfiError;
use crate::protocol::DomainParameters;
/*
pub trait AutomaticEnvironment<DP: DomainParameters>{
    fn run(&mut self) -> Result<(), AmfiError<DP>>;
    fn run_penalise_illegals(&mut self, cheat_penalty: DP::UniversalReward);
}

 */

